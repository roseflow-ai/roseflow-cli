source "https://rubygems.org"

# Specify your gem's dependencies in roseflow-cli.gemspec
gemspec

gem "rake", "~> 12.0"
gem "rspec", "~> 3.0"

eval_gemfile("Gemfile.local") if File.exist?("Gemfile.local")
